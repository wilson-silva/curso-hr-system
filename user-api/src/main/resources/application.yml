server:
  port: ${PORT:8000}
spring:
  application:
    name: user-api
eureka:
  client:
    service-url:
      default-zone: ${eureka_url:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true
    hostname: localhost